---
title: Java Program to Convert Date to String
description: A Java program to convert a Date to a String.
---

## Problem Description

Write a Java program to convert a Date to a String.

## Code

```java title="DateToString.java"
import java.text.DateFormat;
import java.text.SimpleDateFormat;
import java.util.Date;
import java.util.Calendar;

public class DateToString {
    public static void main(String[] args) {
        Date date = Calendar.getInstance().getTime();
        DateFormat dateFormat = new SimpleDateFormat("yyyy-mm-dd hh:mm:ss");
        String strDate = dateFormat.format(date);
        System.out.println("Converted String: " + strDate);
    }
}
```

## Output

```bash
Converted String: 2023-45-27 10:45:30
```

## Explanation

1.  **`format()`**: Formats a Date into a date/time string.
