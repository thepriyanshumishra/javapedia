---
title: Java Program to Calculate Area of Triangle
description: A Java program to calculate the Area of a Triangle.
---

## Problem Description

Write a Java program to calculate the Area of a Triangle.

## Code

```java title="AreaOfTriangle.java"
public class AreaOfTriangle {
    public static void main(String[] args) {
        float b = 4, h = 13, area;
        area = (b * h) / 2;
        System.out.println("Area of Triangle is: " + area);
    }
}
```

## Output

```bash
Area of Triangle is: 26.0
```

## Explanation

1.  **Formula**: `(base * height) / 2`.
